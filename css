:root{--bg:#f6f1e7;--ink:#2a2622;--muted:#6b635a;--accent:#8a3b2a;--card:#fffdf8;--line:#ded5c4;--dark:#22201d}
*{box-sizing:border-box}
html{scroll-behavior:smooth}
body{margin:0;font-family:Georgia,'Times New Roman',serif;background:var(--bg);color:var(--ink);line-height:1.65}
h1,h2,h3{line-height:1.2;font-weight:normal}
h2{font-size:1.9rem;margin:0 0 .3rem}
.wrap{max-width:900px;margin:0 auto;padding:0 20px}
section{padding:64px 0;border-bottom:1px solid var(--line);scroll-margin-top:44px}
.kicker{font:600 .75rem/1 system-ui,sans-serif;letter-spacing:.14em;text-transform:uppercase;color:var(--accent);margin-bottom:10px}
.lead{color:var(--muted);max-width:640px}
nav{position:sticky;top:0;z-index:20;background:rgba(246,241,231,.95);border-bottom:1px solid var(--line);font:.8rem system-ui,sans-serif}
nav .wrap{display:flex;gap:14px;overflow-x:auto;padding:10px 20px;white-space:nowrap;align-items:center}
nav a{color:var(--ink);text-decoration:none}nav a:hover{color:var(--accent)}
nav .sp{flex:1}
button{font:inherit;cursor:pointer}
.btn{font:.8rem system-ui,sans-serif;background:var(--ink);color:#fff;border:0;border-radius:6px;padding:7px 12px}
.btn.alt{background:transparent;color:var(--ink);border:1px solid var(--ink)}
.hero{min-height:80vh;display:flex;align-items:center;background:var(--dark);color:#f3ece0;border:0}
.hero h1{font-size:clamp(2.2rem,6vw,4rem);margin:0 0 16px}
.hero p{color:#c9bfae;max-width:560px}
.hero .kicker{color:#e0a48f}
.grid{display:grid;gap:14px;grid-template-columns:repeat(auto-fit,minmax(200px,1fr))}
.card{background:var(--card);border:1px solid var(--line);border-radius:10px;padding:16px}
.card h3{font-size:1.05rem;margin:0 0 6px}
.chips{display:flex;flex-wrap:wrap;gap:8px;margin:18px 0}
.chip{font:.85rem system-ui,sans-serif;background:var(--card);border:1px solid var(--ink);border-radius:20px;padding:7px 14px;color:var(--ink)}
.chip.on{background:var(--ink);color:#fff}
.panel{background:var(--card);border:1px solid var(--line);border-radius:10px;padding:18px;min-height:110px}
.two{display:grid;gap:14px;grid-template-columns:1fr 1fr}
@media(max-width:640px){.two{grid-template-columns:1fr}}
.yes{border-left:4px solid #3d6b4f}
.no{border-left:4px solid var(--accent)}
.small{font:.8rem system-ui,sans-serif;color:var(--muted)}
.slot{position:relative;background:#e7dfcf;border:2px dashed #b9ad96;border-radius:10px;min-height:200px;display:flex;align-items:center;justify-content:center;text-align:center;overflow:hidden}
.slot img{width:100%;display:block}
.slot .ph{padding:16px;font:.85rem system-ui,sans-serif;color:var(--muted)}
.slot.has{border:0}.slot.has .ph{display:none}
.slot .tools{display:none;position:absolute;top:8px;right:8px;gap:6px;z-index:3}
body.edit .slot .tools{display:flex}
figcaption{font:.8rem system-ui,sans-serif;color:var(--muted);margin-top:6px}
figure{margin:0}
.hot{position:absolute;left:12px;right:12px;bottom:12px;display:flex;flex-wrap:wrap;gap:8px;justify-content:center}
.hot button{background:rgba(34,32,29,.88);color:#fff;border:0;border-radius:20px;padding:8px 14px;font:.8rem system-ui,sans-serif}
.hot button.on{background:var(--accent)}
.q{margin-top:14px;font-size:1.3rem;font-style:italic;min-height:2.6em}
.eq{display:flex;flex-wrap:wrap;gap:10px;align-items:center;justify-content:center;font-size:1.05rem;margin:26px 0}
.eq span.b{background:var(--card);border:1px solid var(--line);padding:12px 16px;border-radius:10px}
.eq .last{background:var(--accent);color:#fff;border-color:var(--accent)}
.item{cursor:pointer}
.item .tag{font:.7rem system-ui,sans-serif;background:#efe3cf;border-radius:4px;padding:2px 6px;color:var(--accent)}
.item .more{display:none;margin-top:8px;font-size:.92rem}
.item.open .more{display:block}
.item.open{border-color:var(--accent)}
.switch{display:inline-flex;border:1px solid var(--ink);border-radius:24px;overflow:hidden;font:.85rem system-ui,sans-serif}
.switch button{border:0;background:transparent;padding:8px 18px}
.switch button.on{background:var(--ink);color:#fff}
.stat{font-size:2.2rem;color:var(--accent)}
blockquote{margin:0;padding:12px 16px;border-left:3px solid var(--accent);background:var(--card);border-radius:0 8px 8px 0}
blockquote cite{display:block;font:.75rem system-ui,sans-serif;color:var(--muted);margin-top:6px;font-style:normal}
.flag{font:.75rem system-ui,sans-serif;background:#fff3cd;border:1px solid #e6d28a;border-radius:6px;padding:8px 10px;margin:12px 0;color:#5c4a00}
#srcList li{margin:8px 0}
#srcList a{color:var(--accent)}
body.edit [data-edit]{outline:1px dashed #c9a27a;outline-offset:2px;background:rgba(255,240,200,.35)}
footer{padding:30px 0;font:.8rem system-ui,sans-serif;color:var(--muted);text-align:center}
:focus-visible{outline:2px solid var(--accent);outline-offset:2px}
